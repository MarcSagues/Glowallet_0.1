  
  $('a#entrar').click(function (e) {
  
    e.preventDefault();                   // prevent default anchor behavior
    var goTo = this.getAttribute("href"); // store anchor href

    setTimeout(function(){
         window.location = goTo;
    },1000);       
});




  $('ul#test1').click(function (e) {
  	    	alert(this.getAttribute("id"));

    e.preventDefault();
    if (this.getAttribute("id") !== "opcion2") {
    	var goTo = $('.botones')[1].getAttribute("href");
    }  else {
    	var goTo = $('.botones')[0].getAttribute("href"); 
}                 
    
    alert(goTo);

    setTimeout(function(){
         window.location = goTo;
    },1000);       
}); 